
    # macro input
    #> $(motion_x)
    #> $(motion_y)
    #> $(motion_z)
    #> $(rotation_y)

    $summon armadillo ~ ~ ~ {Motion:[$(motion_x)d,$(motion_y)d,$(motion_z)d],Rotation:[$(rotation_y)f,0f]}